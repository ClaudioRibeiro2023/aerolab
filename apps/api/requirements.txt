# AeroLab Platform - Requirements
# Python 3.11+ (3.14 compatible)

# Core Framework
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Async & HTTP
aiofiles>=24.1.0
httpx>=0.28.0

# AI/LLM Providers (Python 3.14 compatible)
openai>=1.58.0
anthropic>=0.40.0
groq>=0.13.0
mistralai>=1.2.0

# Database & Storage (lightweight)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.18
pyyaml>=6.0.2
orjson>=3.10.0

# WebSocket
websockets>=14.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Development (optional - comment out if issues)
# black>=24.0.0
# ruff>=0.8.0
# mypy>=1.13.0

# Note: The following packages are NOT compatible with Python 3.14 yet:
# - chromadb (RAG vector store)
# - sentence-transformers (embeddings)
# - langchain (LLM framework)
# - pgvector (PostgreSQL vectors)
# - redis (caching)
# Install these manually when Python 3.14 support is added
